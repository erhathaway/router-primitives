import { RouterAction, RouterReducer } from "../../types";
declare const scene: {
    actions: {
        show: RouterAction;
        hide: RouterAction;
    };
    reducer: RouterReducer;
};
export default scene;
